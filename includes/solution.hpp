#ifndef SOLUTION_HPP
#define SOLUTION_HPP

#include <cctype>
#include <map>
#include <string>
#include <vector>

std::string DecodeMessage(const std::string& encoded,
                          const std::map<std::string, std::string>& mapping);

#endif  // SOLUTION_HPP
